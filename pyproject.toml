[project]
name = "color-correct"
version = "0.1.0"
description = "Color correction learning app with lessons and quizzes"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.0,<4",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
